[package]
name = "waycast-config"
version = "0.1.0"
edition = "2024"

[dependencies]
config = { version = "0.15.15", features = ["toml"] }
directories = "6.0.0"
serde = { version = "1.0.219", features = ["derive"] }
serde_toml = "0.0.1"
toml = "0.9.5"
